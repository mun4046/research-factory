# Watchlist

한 줄에 하나: `티커 | 메모`. 배치(batch)가 여기 있는 종목의 `entity-*` 페이지를 매일 갱신한다.
배치가 발굴한 `candidate` 종목을 계속 추적하려면 이 파일에 추가하면 된다(→ `status: watchlist`).

## KR

005930 | 삼성전자 — 예시

## US

NVDA | NVIDIA — 예시
